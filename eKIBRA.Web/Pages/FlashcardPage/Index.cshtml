@page
@model eKIBRA.Web.Pages.FlashcardPage.IndexModel
@{
    ViewData["Title"] = "List";
}
<h4>Flashcard List</h4>
<p>
    <a asp-page="Create">Create New</a> |
    <a asp-page="./BulkImport">Import CSV</a> |
    <a asp-page="./Index">Back to full List</a>
</p>
<hr />

<partial name="_ValidationAuthenticationCheckPartial" />
<partial name="_ValidationStatusMessagePartial" for="StatusMessage" />

@{
    var prevDisabled = !Model.Data.EntityList.HasPreviousPage ? "disabled" : "";
    var nextDisabled = !Model.Data.EntityList.HasNextPage ? "disabled" : "";
}

<div class="row align-items-center">
    <div class="col">
        <form asp-page="./Index" method="get">
            <div class="row align-items-center">
                <div class="col-md-4">
                    <input asp-for="@Model.Filter.TitleFilter" class="form-control" autocomplete="off" 
                           placeholder="Linked Deck" name="searchTitle" value="@Model.Filter.TitleFilter" />
                </div>
                <div class="col-md-4">
                    <input asp-for="@Model.Filter.QuestionFilter" class="form-control" autocomplete="off" 
                           placeholder="Question" name="searchQuestion" value="@Model.Filter.QuestionFilter" />
                </div>
                <div class="col">
                    <input type="submit" value="Filter" class="btn btn-primary" />
                </div>
            </div>
        </form>
    </div>

    <div class="col-md-auto">
        <a asp-page="./Index"
           asp-route-sortedBy="@Model.Filter.SortedBy"
           asp-route-titleFilter="@Model.Filter.TitleFilter"
           asp-route-questionFilter="@Model.Filter.QuestionFilter"
           asp-route-pageIndex="@(Model.Data.EntityList.PageIndex - 1)"
           class="btn btn-primary @prevDisabled">
            Previous
        </a>

        <button class="btn btn-dark">@Model.Data.EntityList.PageIndex</button>

        <a asp-page="./Index"
           asp-route-sortedBy="@Model.Filter.SortedBy"
           asp-route-titleFilter="@Model.Filter.TitleFilter"
           asp-route-questionFilter="@Model.Filter.QuestionFilter"
           asp-route-pageIndex="@(Model.Data.EntityList.PageIndex + 1)"
           class="btn btn-primary @nextDisabled">
            Next
        </a>
    </div>
</div>
<br />

<div class="table-responsive">
    <table class="table table-hover">
        <thead>
        <tr>
            <th>
                <a asp-page="./Index"
                   asp-route-sortedBy="@Model.Filter.TitleSort"
                   asp-route-titleFilter="@Model.Filter.TitleFilter"
                   asp-route-questionFilter="@Model.Filter.QuestionFilter"
                   asp-route-pageIndex="@Model.Data.EntityList.PageIndex">
                    Linked Deck
                </a>
            </th>
            <th>
                <a asp-page="./Index"
                   asp-route-sortedBy="@Model.Filter.QuestionSort"
                   asp-route-titleFilter="@Model.Filter.TitleFilter"
                   asp-route-questionFilter="@Model.Filter.QuestionFilter"
                   asp-route-pageIndex="@Model.Data.EntityList.PageIndex">
                    Question
                </a>
            </th>
            <th>
                <a asp-page="./Index"
                   asp-route-sortedBy="@Model.Filter.AnwserSort"
                   asp-route-titleFilter="@Model.Filter.TitleFilter"
                   asp-route-questionFilter="@Model.Filter.QuestionFilter"
                   asp-route-pageIndex="@Model.Data.EntityList.PageIndex">
                    Anwser
                </a>
            </th>
            <th>
                <a asp-page="./Index"
                   asp-route-sortedBy="@Model.Filter.CreatedSort"
                   asp-route-titleFilter="@Model.Filter.TitleFilter"
                   asp-route-questionFilter="@Model.Filter.QuestionFilter"
                   asp-route-pageIndex="@Model.Data.EntityList.PageIndex">
                    Created
                </a>
            </th>
            <th>
                <a asp-page="./Index"
                   asp-route-sortedBy="@Model.Filter.ModifiedSort"
                   asp-route-titleFilter="@Model.Filter.TitleFilter"
                   asp-route-questionFilter="@Model.Filter.QuestionFilter"
                   asp-route-pageIndex="@Model.Data.EntityList.PageIndex">
                    Modified
                </a>
            </th>
            <th></th>
        </tr>
        </thead>
        <tbody class="table-group-divider">
        @foreach (var item in Model.Data.EntityList)
        {
            <tr>
                <td>
                    @Html.DisplayFor(modelItem => item.LinkedDeck.Title)
                </td>
                <td>
                    @Html.DisplayFor(modelItem => item.Question)
                </td>
                <td>
                    @Html.DisplayFor(modelItem => item.Answer)
                </td>
                <td>
                    @item.Created.ToString("dd.MMM.yyyy HH:mm")
                </td>
                <td>
                    @item.Modified?.ToString("dd.MMM.yyyy HH:mm")
                </td>
                <td>
                    <a asp-page="./Edit" asp-route-id="@item.Id">Edit</a> |
                    <a asp-page="./Details" asp-route-id="@item.Id">Details</a> |
                    <a asp-page="./Delete" asp-route-id="@item.Id">Delete</a>
                </td>
            </tr>
        }
        </tbody>
    </table>
</div>

<div class="row align-items-center">
    <div class="col"></div>
    <div class="col-md-auto">
        <a asp-page="./Index"
           asp-route-sortedBy="@Model.Filter.SortedBy"
           asp-route-titleFilter="@Model.Filter.TitleFilter"
           asp-route-questionFilter="@Model.Filter.QuestionFilter"
           asp-route-pageIndex="@(Model.Data.EntityList.PageIndex - 1)"
           class="btn btn-primary @prevDisabled">
            Previous
        </a>

        <button class="btn btn-dark">@Model.Data.EntityList.PageIndex</button>

        <a asp-page="./Index"
           asp-route-sortedBy="@Model.Filter.SortedBy"
           asp-route-titleFilter="@Model.Filter.TitleFilter"
           asp-route-questionFilter="@Model.Filter.QuestionFilter"
           asp-route-pageIndex="@(Model.Data.EntityList.PageIndex + 1)"
           class="btn btn-primary @nextDisabled">
            Next
        </a>
    </div>
</div>
